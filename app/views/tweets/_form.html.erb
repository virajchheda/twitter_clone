
<%= simple_form_for(@tweet) do |f| %>
  <%= f.error_notification %>

  <%= f.input :tweet, label:"Tweet about it", placeholder:"Compose a new tweet.....", require:true%>

  <%= f.button :submit, class: "btn-primary" %>
<% end %>
